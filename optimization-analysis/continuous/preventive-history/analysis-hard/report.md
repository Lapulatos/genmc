# P0.4 adapted hard cohort

## Result

The two-repetition matrix contains 240 cells over 60 tasks. It has
0 repeatable resource-to-terminal gains,
0 repeatable new OOM regressions, and
0 safe execution-count mismatches.

Repeatable status differences: `{}`.
